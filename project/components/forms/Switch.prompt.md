Toggle switch (azul when on); optional inline label.

```jsx
import { Switch } from './Switch';
<Switch label="Publicar imediatamente" defaultChecked />
```
Props: `label`. Forwards native checkbox attributes (`role="switch"` applied).

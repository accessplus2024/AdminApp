Styled native `<select>` with a chevron affordance.

```jsx
import { Select } from './Select';
<Select defaultValue="grad">
  <option value="grad">Graduação</option>
  <option value="pos">Pós-graduação</option>
</Select>
```
Forwards native select attributes; pass `<option>`s as children.
